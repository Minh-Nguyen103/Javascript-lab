import { fillPath } from './String-07-fillPath-main';

describe('fillPath(path,params)', () => {
  it('should return empty string when path is empty', () => {});

  it('should return path input when params is empty or does not corresponding value', () => {});

  it('should ', () => {});
});
