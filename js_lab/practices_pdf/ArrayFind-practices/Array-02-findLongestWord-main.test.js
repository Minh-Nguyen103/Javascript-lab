import { findLongestWord } from './Array-02-findLongestWord-main';

describe('findLongestWord(wordList)', () => {
  it('should empty string when wordList not Array or wordList have length = 0', () => {});

  it('should ', () => {
    expect().toBe();
  });
});
