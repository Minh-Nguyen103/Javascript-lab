import { isDecreasingNumberList } from './Array-10-isDecreasingNumberList-main';

describe('isDecreasingNumberList(numberList)', () => {
  it('should return false when numberList is empty', () => {});

  it('should return false when numberList has element that type different Number', () => {});

  it('should return false when numberList not a decreasing number list', () => {});

  it('should return true when numberList a decreasing number list', () => {});
});
